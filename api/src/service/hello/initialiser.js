const initialiser = async () => {
  return `This is the initial value for the message. It was sent by the stack-demo API, specifically the 'hello' service initialiser, at startup.`
}

export default initialiser
