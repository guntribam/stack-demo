
- keeping track of mutable content, like editable entities from db (id only may be not enough)
- evaluate better scrolling, specially to check if the user is not AFK
- tracking clicking element using some sort of 'jquery selector', or similar

